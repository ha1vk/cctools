diff -Nur mc-4.6.2/slang/slsignal.c mc-4.6.2-bionic/slang/slsignal.c
--- mc-4.6.2/slang/slsignal.c	2009-02-02 01:30:21.000000000 +0600
+++ mc-4.6.2-bionic/slang/slsignal.c	2013-10-27 16:11:48.993290430 +0700
@@ -241,7 +241,11 @@
 	(void) sigprocmask (SIG_SETMASK, &save_mask, NULL);
 # endif
 
+#ifdef __BIONIC__
+	execl ("/system/bin/sh", "sh", "-c", cmd, NULL);
+#else
 	execl ("/bin/sh", "sh", "-c", cmd, NULL);
+#endif
 	_exit (127);
      }
    else
diff -Nur mc-4.6.2/slang/slutty.c mc-4.6.2-bionic/slang/slutty.c
--- mc-4.6.2/slang/slutty.c	2013-10-26 14:43:33.000000000 +0700
+++ mc-4.6.2-bionic/slang/slutty.c	2013-10-27 15:32:55.581193692 +0700
@@ -76,7 +76,7 @@
 int SLang_TT_Baud_Rate;
 
 #ifdef HAVE_TERMIOS_H
-# if !defined(HAVE_TCGETATTR) || !defined(HAVE_TCSETATTR)
+# if (!defined(HAVE_TCGETATTR) || !defined(HAVE_TCSETATTR)) && !defined(__BIONIC__)
 #   undef HAVE_TERMIOS_H
 # endif
 #endif
diff -Nur mc-4.6.2/src/cons.handler.c mc-4.6.2-bionic/src/cons.handler.c
--- mc-4.6.2/src/cons.handler.c	2009-02-02 01:30:21.000000000 +0600
+++ mc-4.6.2-bionic/src/cons.handler.c	2013-10-27 15:32:55.585193692 +0700
@@ -131,7 +131,9 @@
 	    /* Close the extra pipe ends */
 	    close (pipefd1[1]);
 	    close (pipefd2[0]);
+#ifndef __BIONIC__
 	    tty_name = ttyname (0);
+#endif
 	    /* Bind the pipe 0 to the standard input */
 	    close (0);
 	    dup (pipefd1[0]);
@@ -143,11 +145,13 @@
 	    /* Bind standard error to /dev/null */
 	    close (2);
 	    open ("/dev/null", O_WRONLY);
+#ifndef __BIONIC__
 	    if (tty_name) {
 		/* Exec the console save/restore handler */
 		mc_conssaver = mhl_str_dir_plus_file (SAVERDIR, "cons.saver");
 		execl (mc_conssaver, "cons.saver", tty_name, (char *) NULL);
 	    }
+#endif
 	    /* Console is not a tty or execl() failed */
 	    console_flag = 0;
 	    write (1, &console_flag, 1);
diff -Nur mc-4.6.2/src/ext.c mc-4.6.2-bionic/src/ext.c
--- mc-4.6.2/src/ext.c	2009-02-02 01:30:21.000000000 +0600
+++ mc-4.6.2-bionic/src/ext.c	2013-10-27 16:21:07.793313596 +0700
@@ -107,7 +107,11 @@
 	return;
     }
     cmd_file = fdopen (cmd_file_fd, "w");
+#ifdef __BIONIC__
+    fputs ("#! /system/bin/sh\n", cmd_file);
+#else
     fputs ("#! /bin/sh\n", cmd_file);
+#endif
 
     prompt[0] = 0;
     for (; *data && *data != '\n'; data++) {
@@ -222,7 +226,11 @@
 	/* Set executable flag on the command file ... */
 	chmod (file_name, S_IRWXU);
 	/* ... but don't rely on it - run /bin/sh explicitly */
+#ifdef __BIONIC__
+	cmd = g_strconcat ("/system/bin/sh ", file_name, (char *) NULL);
+#else
 	cmd = g_strconcat ("/bin/sh ", file_name, (char *) NULL);
+#endif
     }
 
     if (run_view) {
diff -Nur mc-4.6.2/src/main.c mc-4.6.2-bionic/src/main.c
--- mc-4.6.2/src/main.c	2009-02-02 01:30:21.000000000 +0600
+++ mc-4.6.2-bionic/src/main.c	2013-10-27 16:18:27.953306970 +0700
@@ -1775,7 +1775,11 @@
            shell = g_strdup (pwd->pw_shell);
     }
     if (!shell || !*shell)
+#ifdef __BIONIC__
+	shell = "/system/bin/sh";
+#else
 	shell = "/bin/sh";
+#endif
 
     /* This is the directory, where MC was installed, on Unix this is DATADIR */
     /* and can be overriden by the MC_DATADIR environment variable */
diff -Nur mc-4.6.2/src/pipethrough.c mc-4.6.2-bionic/src/pipethrough.c
--- mc-4.6.2/src/pipethrough.c	2009-02-02 01:30:21.000000000 +0600
+++ mc-4.6.2-bionic/src/pipethrough.c	2013-10-27 16:19:44.185310130 +0700
@@ -271,7 +271,11 @@
 {
 	const char *shell = getenv("SHELL");
 	if (shell == NULL) {
+#ifdef __BIONIC__
+		shell = "/system/bin/sh";
+#else
 		shell = "/bin/sh";
+#endif
 	}
 
 	if (close(STDIN_FILENO) == -1
diff -Nur mc-4.6.2/src/user.c mc-4.6.2-bionic/src/user.c
--- mc-4.6.2/src/user.c	2009-02-02 01:30:21.000000000 +0600
+++ mc-4.6.2-bionic/src/user.c	2013-10-27 16:22:04.801315960 +0700
@@ -588,7 +588,11 @@
 	return;
     }
     cmd_file = fdopen (cmd_file_fd, "w");
+#ifdef __BIONIC__
+    fputs ("#! /system/bin/sh\n", cmd_file);
+#else
     fputs ("#! /bin/sh\n", cmd_file);
+#endif
     commands++;
     
     for (col = 0; *commands; commands++){
@@ -663,7 +667,11 @@
     } else {
 	/* execute the command indirectly to allow execution even
 	 * on no-exec filesystems. */
+#ifdef __BIONIC__
+	char *cmd = g_strconcat("/system/bin/sh ", file_name, (char *)NULL);
+#else
 	char *cmd = g_strconcat("/bin/sh ", file_name, (char *)NULL);
+#endif
 	shell_execute (cmd, EXECUTE_HIDE);
 	g_free(cmd);
     }
diff -Nur mc-4.6.2/vfs/sfs.c mc-4.6.2-bionic/vfs/sfs.c
--- mc-4.6.2/vfs/sfs.c	2009-02-02 01:30:21.000000000 +0600
+++ mc-4.6.2-bionic/vfs/sfs.c	2013-10-27 16:23:18.257319005 +0700
@@ -148,7 +148,11 @@
 
     g_free (pqname);
     open_error_pipe ();
+#ifdef __BIONIC__
+    if (my_system (EXECUTE_AS_SHELL, "/system/bin/sh", pad)) {
+#else
     if (my_system (EXECUTE_AS_SHELL, "/bin/sh", pad)) {
+#endif
 	close_error_pipe (1, NULL);
 	return -1;
     }
